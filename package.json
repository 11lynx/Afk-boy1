bot.on('kicked', (reason) => {
  console.log('GEKICKT:', reason);
});

bot.on('error', (err) => {
  console.log('FEHLER:', err);
});

bot.on('end', () => {
  console.log('Disconnected (kein Join möglich)');
  setTimeout(startBot, 5000);
});
